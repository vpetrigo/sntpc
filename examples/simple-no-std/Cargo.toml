[package]
name = "example-simple-no-std"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
sntpc = { path = "../../sntpc", default-features = false }
yash-executor = { version = "1.0.0", default-features = false }
